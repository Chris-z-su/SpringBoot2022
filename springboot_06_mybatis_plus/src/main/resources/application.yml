# 2. 配置相关信息
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wollo?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 1106135

# 3. 设置Mybatis-plus相关配置
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tbl_
